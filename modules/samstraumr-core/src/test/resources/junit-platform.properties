# JUnit Platform Configuration for Samstraumr
# This file configures JUnit 5 test execution behavior

# Parallel execution configuration
# Using 75% of available cores to leave headroom for system processes
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.config.dynamic.factor=0.75

# Test instance lifecycle - per_method creates fresh test instance for each @Test,
# preventing accidental state sharing between tests
junit.jupiter.testinstance.lifecycle.default=per_method

# Display name generation - converts test_method_names to "test method names" in reports
junit.jupiter.displayname.generator.default=org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores

# Tag expressions for test filtering (can be overridden via command line)
# junit.jupiter.execution.tag.include=ATL
# junit.jupiter.execution.tag.exclude=BTL

# Timeout configuration - currently disabled; tests rely on CI-level timeouts
# Uncomment to enforce per-test time limits during local development
# junit.jupiter.execution.timeout.default=5m
# junit.jupiter.execution.timeout.testable.method.default=1m
