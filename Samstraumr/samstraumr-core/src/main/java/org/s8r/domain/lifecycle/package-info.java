/**
 * Domain lifecycle package provides lifecycle state management for domain entities.
 * <p>
 * This package defines the lifecycle states and transitions that domain entities can undergo
 * throughout their existence. It includes state definitions, transition rules, and validation.
 * These lifecycle concepts are central to the domain model and help enforce business rules
 * about how entities can change over time.
 */
package org.s8r.domain.lifecycle;