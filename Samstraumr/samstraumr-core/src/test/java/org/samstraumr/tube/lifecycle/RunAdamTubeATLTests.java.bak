package org.samstraumr.tube.lifecycle;

import org.junit.runner.RunWith;
import io.cucumber.junit.Cucumber;
import io.cucumber.junit.CucumberOptions;

/**
 * JUnit 4 Cucumber test runner for Above The Line (critical) Adam Tube concept tests.
 * 
 * <p>This runner is specifically designed to run critical tests related to the Adam Tube concept,
 * which represents tubes that serve as origin points in the tube hierarchy.
 */
@RunWith(Cucumber.class)
@CucumberOptions(
    features = "src/test/resources/tube/features/L0_Tube/lifecycle/adam-tube-tests.feature",
    glue = {"org.samstraumr.tube.steps", "org.samstraumr.tube.lifecycle.steps", "org.samstraumr.tube.lifecycle.steps.adam"},
    plugin = {
        "pretty",
        "html:target/cucumber-reports/adam-tube-atl.html",
        "json:target/cucumber-reports/adam-tube-atl.json"
    },
    monochrome = true,
    tags = "@ATL and @AdamTube"
)
public class RunAdamTubeATLTests {
    // This class serves as a JUnit 4 cucumber test runner for ATL tests
}